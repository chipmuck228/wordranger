export interface LexemeTags {
  lexemeId: string;
  topics: string[];
  semanticCategories: string[];
  gameTags: string[];
  topicConfidence: number;
  semanticConfidence: number;
  gameConfidence: number;
}
