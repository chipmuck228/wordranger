/**
 * Matching V1 respects PublicLearningTask option order.
 * Visual placement must stay independent of correctness.
 */
export function matchingOptionOrder(
  optionIds: string[],
): string[] {
  return [...optionIds];
}
